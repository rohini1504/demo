# Task Manager API

Simple FastAPI-based backend for managing tasks.

## Run locally

pip install -r requirements.txt
uvicorn app.main:app --reload

## Endpoints

- POST /tasks
- GET /tasks
- PUT /tasks/{id}
